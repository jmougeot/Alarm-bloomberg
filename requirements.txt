# Strategy Price Monitor - Dependencies

# GUI Framework (Qt pour Python)
PySide6
# Graphiques temps réel (optionnel, pour futures extensions)
pyqtgraph

# Utilitaires
numpy

# WebSocket pour le serveur d'alarmes
websockets
httpx
aiofiles

# Bloomberg API (installer séparément depuis Bloomberg SDK)
# pip install --index-url=https://bcms.bloomberg.com/pip/simple blpapi
# ou
# $env:BLPAPI_ROOT = "C:\blp\DAPI"
# pip install blpapi
